command_timeout = 750

[battery]
disabled = true

[c]
commands = [["$CC", "--version"], ["clang", "--version"], ["gcc", "--version"]]
format = 'via [$symbol($name( $version)) ]($style)'

[cmd_duration]
disabled = true

[dotnet]
disabled = true

[package]
disabled = true

[python]
python_binary = ["py"]
detect_folders = [".venv"]
